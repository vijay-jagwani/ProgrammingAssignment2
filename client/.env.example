# Copy to .env.local for local dev against your Supabase project.
# Leave unset to use the local in-memory dev harness (npm run dev).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set to "local" to force the local harness even when the above are set.
VITE_BACKEND=
